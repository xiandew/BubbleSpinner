export default class Hole {
        constructor(x = 0, y = 0, layer = 0) {
                this.x = x;
                this.y = y;
                this.layer = layer;
                this.filled = false;
        }
}